apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-db
  labels:
    app: task-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-db
  template:
    metadata:
      labels:
        app: task-db
    spec:
      containers:
        - name: task-db
          image: postgres:16
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: task_db
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: task-tracker-config
                  key: TASK_DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: task-tracker-secrets
                  key: TASK_DB_PASSWORD
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "task", "-d", "task_db"]
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: task-db-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: task-db-data
          persistentVolumeClaim:
            claimName: task-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: task-db
  labels:
    app: task-db
spec:
  selector:
    app: task-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
